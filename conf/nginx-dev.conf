    #generated by `lor framework`
    
# user www www;
pid tmp/nginx.pid;

# This number should be at maxium the number of CPU on the server
worker_processes 4;

events {
    # Number of connections per worker
    worker_connections 4096;
}

http {
    # use sendfile
    sendfile on;
    include /data/software/openresty/nginx/conf/mime.types;

    # lor initialization
    lua_package_path "./app/?.lua;./app/library/?.lua;./app/?/init.lua;./?.lua;/data/tmp/lua_framework/github_lor/?.lua;/data/tmp/lua_framework/github_lor/?/init.lua;;";
    lua_package_cpath "./app/library/?.so;/data/tmp/lua_framework/github_lor/?.so;;";
    lua_code_cache off;
    #LUA_SHARED_DICT


    
    
    
    

    server {
        # List port
        listen 8888;
        set $template_root '';

        location /static {
            alias ./app/static; #app/static;
        }

        # Access log with buffer, or disable it completetely if unneeded
        access_log logs/dev-access.log combined buffer=16k;
        # access_log off;

        # Error log
        error_log logs/dev-error.log;

        # lor runtime
        location / {
            content_by_lua_file ./app/main.lua;
        }
    }
}

    